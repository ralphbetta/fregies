import 'package:fregies/constants/values.dart';
import 'package:fregies/models/product_model.dart';

// void accumulatePrice() {
//   Accumulated_cart_price = 0;
//   for (var i = 0; i < cart.length; i++) {
//     //sum += double.parse(cart[i]['item']['price']);
//     Accumulated_cart_price += (double.tryParse(cart[i]['item']['price'])! *
//         double.tryParse(cart[i]['quantity'].toString())!);
//   }
//   print(Accumulated_cart_price);
//   print("total price");
// }

// bool inCart(ProductModel product) {
//   List pro = cart.where((element) => element['item'] == product).toList();
//   if (pro.isNotEmpty) {
//     return true;
//   } else {
//     return false;
//   }
// }
